version: 2
jobs:
  build:
    docker:
      - image: cirrusci/flutter
    steps:
      - checkout
      - run:
          working_directory: app
          command: |
            flutter doctor
            flutter test
            flutter -v build apk
